pub mod stats;
// use crate::stats::median;
// use crate::stats::mode;
pub mod pig;
use crate::pig::latin;

fn main(){
    // let mut v = vec![2, 1, 2, 3];
    // println!("{}, {}", mode(&mut v).0, mode(&mut v).1);


}
